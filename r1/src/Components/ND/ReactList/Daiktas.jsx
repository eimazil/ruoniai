function Daiktas({ id, type, name, color }) {
  return (
    <div className="bala">
      <span>{id}</span>
      <span>{type}</span>
      <span>{name}</span>
      <span>{color}</span>
    </div>
  );
}
export default Daiktas;
